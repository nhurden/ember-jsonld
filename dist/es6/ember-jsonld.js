require('../ember-jsonld.js');

export default {
  expand: expand,
  compact: compact,
  flatten: flatten,
  frame: frame,
  fromRDF: fromRDF,
  toRDF: toRDF,
  normalize: normalize
};

export var expand    = expand;
export var compact   = compact;
export var flatten   = flatten;
export var frame     = frame;
export var fromRDF   = fromRDF;
export var toRDF     = toRDF;
export var normalize = normalize;
